{
  "name": "kap-contracts",
  "version": "1.0.0",
  "main": "assembly/index.ts",
  "license": "MIT",
  "author": "Luke Willis <luke@thekoinpress.com>",
  "repository": "https://github.com/lukemwillis/kap-contracts",
  "dependencies": {},
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.51.0",
    "@typescript-eslint/parser": "^5.51.0",
    "dotenv": "^16.0.3",
    "eslint": "^8.34.0",
    "koilib": "^5.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "lint": "eslint --max-warnings 0 --ext ts \"contracts/*/assembly/**/*.ts\"",
    "test-domain:build": "cd contracts/name-service && yarn install && yarn build:release",
    "usd-oracle:build": "cd contracts/usd-oracle && yarn install && yarn build:release",
    "koin-domain:build": "cd contracts/koin-domain && yarn install && yarn build:release && yarn test",
    "koin-domain:ci": "cd contracts/koin-domain && yarn ci",
    "name-service:build": "cd contracts/name-service && yarn install && yarn build:release && yarn test",
    "name-service:ci": "cd contracts/name-service && yarn ci",
    "all:build": "yarn test-domain:build && yarn usd-oracle:build && yarn koin-domain:build && yarn name-service:build",
    "all:ci": "koin-domain:ci && yarn name-service:ci",
    "ci": "yarn lint && yarn all:build && yarn all:ci"
  }
}
