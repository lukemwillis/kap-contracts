{
  "name": "kap-contracts",
  "version": "1.0.0",
  "main": "assembly/index.ts",
  "license": "MIT",
  "author": "Luke Willis <luke@thekoinpress.com>",
  "repository": "https://github.com/lukemwillis/kap-contracts",
  "dependencies": {
    "koinos-proto-js": "^0.0.3",
    "local-koinos": "^0.0.5"
  },
  "devDependencies": {
    "@as-covers/core": "^0.2.1",
    "@as-pect/cli": "^6.2.4",
    "@roaminroe/as-proto-gen": "^0.4.2",
    "@roaminroe/protoc": "^2.4.0",
    "@types/jest": "^27.5.1",
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "@typescript-eslint/parser": "^5.10.0",
    "assemblyscript": "^0.19.22",
    "eslint": "^8.7.0",
    "jest": "^28.1.0",
    "koinos-abi-proto-gen": "^0.1.11",
    "koinos-as-gen": "^0.4.8",
    "koinos-mock-vm": "npm:@roaminroe/koinos-mock-vm@^1.1.3",
    "koinos-sdk-as": "https://github.com/koinos/koinos-sdk-as#testnet-4",
    "koinos-sdk-as-cli": "^0.1.16",
    "ts-jest": "^28.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.6.4",
    "yaml": "^2.0.0-11"
  },
  "scripts": {
    "lint": "eslint --max-warnings 0 --ext ts \"contracts/*/assembly/**/*.ts\"",
    "buildManaStation": "cd contracts/mana-station && yarn install && yarn build:release && yarn test",
    "buildManaStation:ubuntu": "cd contracts/mana-station && yarn install && yarn build:release && yarn test && yarn integration-tests",
    "buildGovernance": "cd contracts/governance && yarn install && yarn build:release && yarn test",
    "buildGovernance:ubuntu": "cd contracts/governance && yarn install && yarn build:release && yarn test && yarn integration-tests",
    "buildNameService": "cd contracts/name-service && yarn install && yarn build:release && yarn test",
    "buildNameService:ubuntu": "cd contracts/name-service && yarn install && yarn build:release && yarn test && yarn integration-tests",
    "buildKAPToken": "cd contracts/kap-token && yarn install && yarn build:release && yarn test",
    "buildKAPToken:ubuntu": "cd contracts/kap-token && yarn install && yarn build:release && yarn test && yarn integration-tests",
    "buildAll": "yarn buildManaStation && yarn buildGovernance && yarn buildNameService && yarn buildKAPToken",
    "buildAll:ubuntu": "yarn buildManaStation:ubuntu && yarn buildGovernance:ubuntu && yarn buildNameService:ubuntu && yarn buildKAPToken:ubuntu",
    "ci": "yarn buildAll && yarn lint"
  }
}
